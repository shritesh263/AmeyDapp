/**
 * ClubState — superseded by ContractState for BlackBox AI.
 * Kept as an empty stub so the file doesn't cause import errors elsewhere.
 */
export default function ClubState(_props: { contractAddress: string | null }) {
  return null;
}
